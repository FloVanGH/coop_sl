import { LabelMedium, LabelLarge, co } from "../_imports/co_widgets.slint";

export RowTitle := LabelLarge {
    height: 18px;
}

export ColumnTitle := LabelMedium {
    height: 14px;
    color: co.brushes.foreground_disabled;
}