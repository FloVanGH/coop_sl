import { settings } from "../../settings.slint"; 

import { HeaderBar, OutlineButton, CheckBox, Switch, co, mi } from "../../_imports/co_widgets.slint";

export PageMinimized := GridLayout {
    Flickable {  
        @children
    }   
}