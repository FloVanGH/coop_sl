import { CoWindow, Button , co } from "../../../widgets/co_widgets/co_widgets/co_widgets.slint";

export Widgets := CoWindow {
    width: 600px;
    height: 400px;
    title: "Widgets gallery";

    HorizontalLayout {  
        padding: 8px;

        Button {
            text: "light / dark";
            clicked => { co.dark-theme = !co.dark-theme; }
        }
    }
}  