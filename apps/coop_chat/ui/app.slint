// SPDX-FileCopyrightText: 2022 Florian Blasius <co_sl@tutanota.com>
// SPDX-License-Identifier: GPL-3.0-only

import { CoopWindow, GroupListViewItem, ListViewItem } from "_imports/coop-widgets.slint";
import { SideBarViewAdapter, SideBarView } from "side-bar-view.slint";
import { MessageView } from "message-view.slint";

export { SideBarViewAdapter, GroupListViewItem, ListViewItem }

export component App inherits CoopWindow {
    title: "coop_chat";

    min-width: 600px;
    min-height: 400px;

    GridLayout {

        MessageView {
            col: 1;
        }
        SideBarView {
            col: 0;
            responsive: true;
            parent_width: root.width;
        }
    }
}
