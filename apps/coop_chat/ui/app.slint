// SPDX-FileCopyrightText: 2022 Florian Blasius <co_sl@tutanota.com>
// SPDX-License-Identifier: GPL-3.0-only

import { CoopWindow, GroupItemModel, ItemModel } from "_imports/coop_widgets.slint";
import { SideBarViewAdapter, SideBarView } from "side_bar_view.slint";
import { MessageView } from "message_view.slint"; 

export { SideBarViewAdapter, GroupItemModel, ItemModel }

export component App inherits CoopWindow {
    title: "coop_chat";

    min-width: 600px;
    min-height: 400px;

    GridLayout { 
       
        MessageView {
            col: 1;
        }
        SideBarView {
            col: 0;
            responsive: true;
            parent_width: root.width;
        }
    }
}
