// SPDX-FileCopyrightText: 2023 Florian Blasius <co_sl@tutanota.com>
// SPDX-License-Identifier: GPL-3.0-only

import { CoopWindow, Icons, Palette, Icons } from "_imports/coop-widgets.slint";
export { Icons }

import { FilesView, FilesAdapter } from "views/files-view.slint";
export { FilesAdapter }

import { DialogView, DialogViewAdapter } from "views/dialog-view.slint";
export { DialogViewAdapter }

import { SideBarView, SideBarAdapter } from "views/side-bar-view.slint";
export { SideBarAdapter }

// export component Logo {
//     Rectangle {
//         border-width: 1px;
//         border-color: Palette.border;
//         background: Palette.background;
//         width: 128px;
//         height: 128px;
//         border-radius: 24px;

//         Image {
//             width: 80px;
//             source: Icons.folder;
//             colorize: Palette.accent;
//         }
//     }
// }

export component MainWindow inherits CoopWindow {
    title: "coop local";
    min-width: 316px;
    min-height: 400px;
    preferred-width: 600px;

    GridLayout {
        Rectangle {
            col: 1;

            FilesView {
                width: 100%;
                height: 100%;
            }

            DialogView {
                width: 100%;
                height: 100%;
            }
        }

        SideBarView {
            col: 0;
            parent-width: root.width;
            responsive: true;
        }
    }
}