// Internal helper struct to manage the brushes of the  `co` theme.
export struct Brushes := {
    background: brush,
    foreground: brush,
    surface: brush,
    on_surface: brush,
    primary: brush,
    border: brush,
    state: brush,
}