// Internal helper struct to manage opacities of the  `co` theme.
export struct Opacities := {
    state_hover: float,
    state_pressed: float,
    container_disabled: float,
    content_disabled: float,
}