// SPDX-FileCopyrightText: 2022 Florian Blasius <flovanpt@posteo.de>
// SPDX-License-Identifier: MIT

// Internal helper struct to manage border radius of the  `co` theme.
export struct Radius := {
    extra_small: length,
    small: length,
    medium: length,
    large: length,
}