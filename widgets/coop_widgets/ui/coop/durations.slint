// SPDX-FileCopyrightText: 2022 Florian Blasius <co_sl@tutanota.com>
// SPDX-License-Identifier: MIT

// `Durations` provides all animation duration of `Theme`.
export struct Durations := {
    // The animation runs slow.
    slow: duration,

    // The animation runs medium fast.
    medium: duration,

    // The animation runs fast.
    fast: duration,
}